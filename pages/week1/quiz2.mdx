# Quiz 2

## Question 1

```js
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

console.log(functions[2](1,1,1));
```

## Expected Output: 2

## Question 2

```js
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

console.log(functions[3](1,1));
```

## Expected Output: 3

## Question 3

```js
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

console.log(functions[1](1));
```

## Expected Output: 1
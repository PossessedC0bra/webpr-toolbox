import Question from "../../components/Question";
import {Code} from "nextra/components";

# Quiz 2

## Question 1

<Question
    question={<>Shows <Code>2</Code> ?</>}
    code='function one(x) {return x;} function two(x, y){ return x+y; } function three(x, y, z) { return x + y + z; } const functions = [null, one, two ,three]; functions[2](1, 1, 1);'
    expectedValue={2}
>
```js copy showLineNumbers filename="JavaScript"
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

document.writeln(functions[2](1,1,1));
```
</Question>

## Question 2

<Question
    question={<>Shows <Code>3</Code> ?</>}
    code='function one(x) {return x;} function two(x, y){ return x+y; } function three(x, y, z) { return x + y + z; } const functions = [null, one, two ,three]; functions[3](1, 1);'
    expectedValue={3}
>
```js copy showLineNumbers filename="JavaScript"
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

document.writeln(functions[3](1,1));
```
</Question>

## Question 3

<Question
    question={<>Shows <Code>1</Code> ?</>}
    code='function one(x) {return x;} function two(x, y){ return x+y; } function three(x, y, z) { return x + y + z; } const functions = [null, one, two ,three]; functions[1](1);'
    expectedValue={1}
>
```js copy showLineNumbers filename="JavaScript"
function one(x) {
    return x;
}
function two(x, y) {
    return x + y;
}
function three(x, y, z) {
    return x + y + z;
}

const functions = [null, one, two, three];

document.writeln(functions[1](1));
```
</Question>
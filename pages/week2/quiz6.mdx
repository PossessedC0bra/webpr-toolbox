import TrueFalseQuestion from "../../components/TrueFalseQuestion";
import {Code} from "nextra/components";

# Quiz 6

## Question 1

<TrueFalseQuestion question={<>Are <Code>id1</Code> and <Code>id2</Code> alpha-equivalent?</>} answer={true}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const id1 = x => x;
    const id2 = y => y;
    ```
</TrueFalseQuestion>

## Question 2

<TrueFalseQuestion question={<>Given <Code>id</Code>, is <Code>a1</Code> a proper beta expansion of <Code>a2</Code>?</>} answer={true}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const id = x => x;

    const a1 = y => id(y);
    const a2 = y => y;
    ```
</TrueFalseQuestion>

## Question 3

<TrueFalseQuestion question={<>Given <Code>id</Code>, is <Code>a2</Code> a proper beta reduction of <Code>a1</Code>?</>} answer={true}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const id = x => x;

    const a1 = y => id(y);
    const a2 = y => y;
    ```
</TrueFalseQuestion>

## Question 4

<TrueFalseQuestion question={<>Given <Code>id</Code> and <Code>konst</Code>, is <Code>F2</Code> a proper eta reduction of<Code>F1</Code>?</>} answer={true}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const id = x => x;
    const konst = x => y => x;

    const F1 = x => y => konst(id)(x)(y);
    const F2 = x => konst(id)(x);
    const F3 = konst(id);
    ```
</TrueFalseQuestion>


## Question 5

<TrueFalseQuestion question={<>Given <Code>id</Code> and <Code>konst</Code>, is <Code>F3</Code> a proper eta reduction of<Code>F2</Code>?</>} answer={true}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const id = x => x;
    const konst = x => y => x;

    const F1 = x => y => konst(id)(x)(y);
    const F2 = x => konst(id)(x);
    const F3 = konst(id);
    ```
</TrueFalseQuestion>

## Question 6

<TrueFalseQuestion question={<>Are <Code>F1</Code> and <Code>F2</Code> alpha-equivalent?</>} answer={false}>
    ```javascript copy showLineNumbers filename="JavaScript"
    const F1 = x => (x => x);
    const F2 = y => (x => y);
    ```
</TrueFalseQuestion>
